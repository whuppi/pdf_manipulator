// Package version — always 0.0.0 in git.
// CI stamps the real version from CHANGELOG.md at publish time.
/// The package version. Always 0.0.0 in git; CI stamps the real value.
const packageVersion = '2.1.3-dev.0';
