name: pdf_manipulator_example
description: Example app demonstrating pdf_manipulator features.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  pdf_manipulator:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  webdriver: ^3.0.0

flutter:
  uses-material-design: true
