# Full test — maintainer adds "ready-to-test" label to trigger.
# Label auto-removed on new pushes (stale results = wasted compute).
#
# Package tests: unit + native ops on macOS/Linux/Windows + web (3 modes)
# Integration tests: example app on macOS/Android emulator/iOS simulator + web (3 modes)
# Release-build tests: flutter build --release on all 6 targets
#
# Every job composes reusable actions from .github/actions/.
name: Full Test

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
    branches: [prod, dev]

concurrency:
  group: full-test-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # ── Package tests (dart test) ──────────────────────────────────────

  pkg-macos:
    name: "pkg: macOS"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: macos
          make-target: test-pkg-native

  pkg-linux:
    name: "pkg: Linux"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: linux
          make-target: test-pkg-native

  pkg-windows:
    name: "pkg: Windows"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: windows
          make-target: test-pkg-native

  pkg-web:
    name: "pkg: Web (3 modes)"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: web
          make-target: test-ops-web

  # ── Integration tests (flutter test on device) ─────────────────────

  int-macos:
    name: "integration: macOS"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: macos
          make-target: test-example-macos

  int-linux:
    name: "integration: Linux"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: linux
          make-target: test-example-linux
          xvfb: 'true'

  int-windows:
    name: "integration: Windows"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: windows
          make-target: test-example-windows

  int-android:
    name: "integration: Android"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: android
          make-target: test-example-android
          emulator: 'true'

  int-ios:
    name: "integration: iOS"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: macos-14
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: ios
          make-target: test-example-ios
          simulator: 'true'

  int-web:
    name: "integration: Web (3 modes)"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: web
          make-target: test-example-web

  # ── Release-build tests ──────────────────────────────────────────
  # Debug and release builds exercise different code paths in the
  # build hook. These verify the hook works in release mode too.

  release-android:
    name: "release: Android (${{ matrix.os }})"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: android
          make-target: test-release-android

  release-ios:
    name: "release: iOS"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: macos-14
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: ios
          make-target: test-release-ios

  release-macos:
    name: "release: macOS"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: macos-14
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: macos
          make-target: test-release-macos

  release-linux:
    name: "release: Linux"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: linux
          make-target: test-release-linux

  release-windows:
    name: "release: Windows"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: windows
          make-target: test-release-windows

  release-web:
    name: "release: Web (${{ matrix.os }})"
    if: github.event.action == 'labeled' && github.event.label.name == 'ready-to-test'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with: { submodules: recursive }
      - uses: ./.github/actions/flutter-target
        with:
          target: web
          make-target: test-release-web

  # ── Gate ───────────────────────────────────────────────────────────

  gate:
    name: Full Test Gate
    if: always()
    needs: [pkg-macos, pkg-linux, pkg-windows, pkg-web, int-macos, int-linux, int-windows, int-android, int-ios, int-web, release-android, release-ios, release-macos, release-linux, release-windows, release-web]
    runs-on: ubuntu-latest
    steps:
      - name: Evaluate
        run: |
          if [[ "${{ github.event.action }}" != "labeled" ]] || [[ "${{ github.event.label.name }}" != "ready-to-test" ]]; then
            echo "Full test not triggered — gate passes"
            exit 0
          fi
          RESULTS=(
            "${{ needs.pkg-macos.result }}"
            "${{ needs.pkg-linux.result }}"
            "${{ needs.pkg-windows.result }}"
            "${{ needs.pkg-web.result }}"
            "${{ needs.int-macos.result }}"
            "${{ needs.int-linux.result }}"
            "${{ needs.int-windows.result }}"
            "${{ needs.int-android.result }}"
            "${{ needs.int-ios.result }}"
            "${{ needs.int-web.result }}"
            "${{ needs.release-android.result }}"
            "${{ needs.release-ios.result }}"
            "${{ needs.release-macos.result }}"
            "${{ needs.release-linux.result }}"
            "${{ needs.release-windows.result }}"
            "${{ needs.release-web.result }}"
          )
          for r in "${RESULTS[@]}"; do
            if [[ "$r" != "success" ]]; then
              echo "FAILED: a test returned '$r'"
              exit 1
            fi
          done
          echo "All jobs passed"
# CI fixes
