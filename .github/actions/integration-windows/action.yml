name: Integration Test (Windows)
description: Run example Flutter integration test on Windows desktop via Makefile

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup-fvm
    - uses: ./.github/actions/setup-rust
    - shell: bash
      run: make test-example-windows
